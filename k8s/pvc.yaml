apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: engligo-data-pvc
  labels:
    app: engligo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard